// function expression 
// função anônima - function anonymous

// parâmetros da função (parameters)
const sum = function(number1, number2) { 
    console.log(number1 + number2)  // number1 numer2 são parâmetros
}

sum(2, 3) // 2 3 são argumentos - arguments