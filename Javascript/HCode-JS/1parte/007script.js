(function (x1,x2, operator) {

    return eval(`${x1} ${operator} ${x2}`);

})(2,2, "*");





// função anônima precisa utilizar (entre a função) e ela pode ser chamada assim que ela é criada porém após chamada deixa de existir



